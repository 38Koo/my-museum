import * as stylex from "@stylexjs/stylex";

export const mainStyle = stylex.create({
  base: {
    height: "30rem",
    position: "relative",
    borderTopWidth: "8px",
    borderTopStyle: "solid",
    borderTopColor: "gray",
  },
});
